@model Vehicle

<div class="text-center">
    <h1>Monthly Vehicle</h1>
    <ul class="list-group">
        <li class="list-group-item list-group-item-heading list-group-item-success">
            <h5>
                <a asp-controller="Vehicle" 
                   asp-action="Details"                   
                   asp-route-id="@Model.VehicleId" 
                   asp-route-slug="@Model.Title.Slug()">
                    @Model.Title
                </a>
            </h5>
        </li>
    </ul>
</div>